{
  "version": 2,
  "functions": {
    "index.js": {
      "maxDuration": 30
    }
  },
  "buildCommand": "chmod +x build.sh && ./build.sh && npm run build",
  "installCommand": "npm install",
  "builds": [
    { "src": "index.js", "use": "@vercel/node" }
  ]
}
